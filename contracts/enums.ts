export enum ApiRequestState {
  PENDING   = "PENDING",
  SUCCESS   = "SUCCESS",
  ERROR     = "ERROR",
}

export enum SelectLeader {
  article         = "Article",
  positionSummary = "Position Summary",
  wholeList       = "WholsList",
}